"""Deep Discussion Mode: structured multi-agent reasoning sessions.

This package provides session management and configuration for deep discussion
sessions, where users can choose an orchestration pattern (sequential, debate,
or peer review) and optionally customise the model assigned to each agent role.
"""
