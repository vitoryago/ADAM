# ADAM Python Backend Requirements
# Install with: pip install -r requirements.txt

# Core dependencies
numpy>=1.21.0
scipy>=1.7.0

# LLM and embedding integrations
openai>=1.3.0
anthropic>=0.8.0
sentence-transformers>=2.2.0
xai-sdk>=0.0.1a8  # For Grok models

# Vector database and search
chromadb>=0.4.0
rank-bm25>=0.2.2

# Graph operations for memory network
networkx>=3.0

# Web framework for API endpoints (optional)
fastapi>=0.100.0
uvicorn>=0.23.0

# Data manipulation and analysis
pandas>=1.5.0

# Visualization (for cost monitoring dashboard)
matplotlib>=3.6.0
plotly>=5.15.0

# Configuration management
python-dotenv>=1.0.0

# Rich output formatting
rich>=13.0.0

# State machine (for LangGraph integration)
langgraph>=0.0.40
langchain>=0.1.0
langchain-community>=0.0.20
langchain-openai>=0.0.8
langchain-anthropic>=0.1.0

# HTTP client for real-time pricing
aiohttp>=3.8.0

# Type hints and validation
pydantic>=2.0.0
typing-extensions>=4.5.0

# Screen capture and vision capabilities
mss>=9.0.1  # Cross-platform screen capture
Pillow>=10.0.0  # Image processing
pytesseract>=0.3.10  # OCR for text extraction

# macOS screen capture (optional, for macOS users)
# pyobjc-framework-Quartz>=10.0  # Uncomment for macOS
# pyobjc-framework-AppKit>=10.0  # Uncomment for macOS

# Testing (optional)
pytest>=7.0.0
pytest-asyncio>=0.21.0